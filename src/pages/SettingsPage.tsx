import { Database } from "lucide-react";
import { useState } from "react";
import { ConfirmDialog, StatusBanner } from "@/components/patterns";
import { CacheDetailsDialog, OfficialApiPanel, SettingsHeader, SettingsRow, ShortcutInput, useSettingsController, WebGatewayPanel } from "@/components/settings";
import { Button, FormField, Input, Select, Spinner, Switch } from "@/components/ui";
import { TARGET_LANGUAGES, type BackendMode } from "@/types";

export function SettingsPage({ onBack, onCacheCleared }: { onBack: () => void; onCacheCleared?: () => void }) {
  const c=useSettingsController(); const [cacheOpen,setCacheOpen]=useState(false);
  return <div className="flex h-full flex-col"><SettingsHeader onBack={onBack}/><main className="flex-1 overflow-y-auto px-4 py-4"><div className="mx-auto max-w-xl space-y-5">{c.loadingConfig?<p className="flex gap-2 text-sm text-ink-muted"><Spinner/>正在加载配置…</p>:null}{c.loadError?<StatusBanner tone="danger" announcement="assertive" description={`加载配置失败：${c.loadError}（将使用默认配置）`}/>:null}<FormField label="翻译后端" hint="Official API 使用付费接口；WebGateway 为实验功能"><Select value={c.config.backendMode} onChange={(event)=>c.changeBackend(event.target.value as BackendMode)}><option value="officialApi">Official API（付费）</option><option value="webGateway">Qwen 网页实验模式</option></Select></FormField>{c.isWebGateway?<WebGatewayPanel config={c.config} setConfig={c.setConfig} status={c.loginStatus} pending={c.loginActionPending} onLogin={()=>void c.beginLogin()} onLogout={()=>c.setLogoutIntent(true)}/>:<OfficialApiPanel config={c.config} setConfig={c.setConfig} onProviderChange={c.changeProvider} onApiKeyChange={c.changeApiKey}/>}<FormField label="全局快捷键" hint="有选区时翻译，无选区时显示翻译窗口；默认 Ctrl+T，可能与其他软件冲突"><ShortcutInput value={c.config.shortcut} onChange={(shortcut)=>c.setConfig({shortcut})}/></FormField><FormField label="目标语言"><Select value={c.config.targetLanguage} onChange={(event)=>c.setConfig({targetLanguage:event.target.value})}>{TARGET_LANGUAGES.map((item)=><option key={item.value} value={item.value}>{item.label}</option>)}</Select></FormField><div className="grid grid-cols-2 gap-4"><FormField label="请求超时（秒）" hint="5～300"><Input type="number" min={5} max={300} value={c.config.timeoutSeconds} onChange={(event)=>c.setConfig({timeoutSeconds:Number(event.target.value)||60})}/></FormField><FormField label="最大翻译字符数" hint="默认 5000"><Input type="number" min={100} max={20000} value={c.config.maxTextLength} onChange={(event)=>c.setConfig({maxTextLength:Number(event.target.value)||5000})}/></FormField></div><div className="space-y-3 rounded-lg border border-line bg-surface-soft/40 px-3 py-3"><SettingsRow title="启用思考模式" description="关闭（默认）省 token、更快；开启可在复杂语境下提升译文质量" control={<Switch checked={c.config.enableThinking} onCheckedChange={(enableThinking)=>c.setConfig({enableThinking})} aria-label="启用思考模式"/>}/><SettingsRow title="流式输出" description="生成时逐步显示译文；Official API 端点需支持标准流式响应" control={<Switch checked={c.config.streamOutput} onCheckedChange={(streamOutput)=>c.setConfig({streamOutput})} aria-label="流式输出"/>}/><SettingsRow title="自动隐藏窗口" description="失去焦点后隐藏临时窗口" control={<Switch checked={c.config.autoHide} onCheckedChange={(autoHide)=>c.setConfig({autoHide})} aria-label="自动隐藏窗口"/>}/><SettingsRow title="默认常驻窗口" description="首次触发时即固定窗口" control={<Switch checked={c.config.pinnedByDefault} onCheckedChange={(pinnedByDefault)=>c.setConfig({pinnedByDefault})} aria-label="默认常驻窗口"/>}/></div><SettingsRow title="翻译缓存" description="查看本机缓存的条目、磁盘占用、命中率和保存位置" control={<Button variant="outline" onClick={()=>setCacheOpen(true)}><Database/>查看缓存详情</Button>}/><div className="flex gap-2"><Button variant="primary" onClick={()=>void c.save()} loading={c.saving}>保存</Button><Button variant="outline" onClick={()=>void c.test()} loading={c.testing==="testing"}>测试连接</Button></div>{c.testMessage?<StatusBanner tone={c.testing==="ok"?"success":"danger"} announcement="polite" description={c.testMessage}/>:null}{c.saveMessage?<StatusBanner tone={c.saveError?"danger":"success"} announcement="polite" description={c.saveMessage}/>:null}</div></main><CacheDetailsDialog open={cacheOpen} onClose={()=>setCacheOpen(false)} onCacheCleared={onCacheCleared}/><ConfirmDialog open={c.logoutIntent} title="确定退出 Qwen 登录？" description="退出后会删除本地保存的登录凭证和 Qwen 浏览器 profile，直到重新登录前翻译不可用。" confirmLabel="退出登录" cancelLabel="取消" tone="danger" pending={c.loginActionPending} onCancel={()=>c.setLogoutIntent(false)} onConfirm={()=>void c.logout()}/></div>;
}
